server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: rentease
  datasource:
    # Changed port from 6543 to 5432 for Session Mode stability
    url: jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.uabtldpqfbkkwmqxtzmq
    password: xEq2TOXrJOAFWn7q
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        default_schema: public
        # Using the explicit dialect helps prevent startup probing errors
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always

app:
  jwt:
    secret: ${JWT_SECRET:RentEaseJwtSecretKeyForSigningTokensMinimum256BitsRequiredForHS256}
    access-expiration-ms: 3600000
    refresh-expiration-ms: 604800000